<!--pages/verifyBook/verifyBook.wxml-->
<view>
  <view class='verifyBookInfo' wx:if="{{booksInfo.length}}">
    <block wx:for="{{booksInfo}}" wx:key="index">
      <view class='bookItem'>
        <view class='bookimg'>
          <image src='{{item.image}}'></image>
        </view>
        <view class='bookinfo'>
          <text class='bookname'>{{item.bname}}</text>
          <text>作者：{{item.author}}</text>
          <text>出版社：{{item.publish}}</text>
          <text>定价：{{item.price}}元</text>
          <text>ISBN：{{item.ISBN}}</text>
          <view class='shell'>
            <text>贝壳：</text>
            <input bindinput='inputShell'></input>
          </view>
        </view>
        <view class='pass_btn'>
          <button bindtap='passVerify' data-uid="{{item.uid}}" data-bid="{{item.bid}}" data-status="1">审核通过</button>
        </view>
      </view>
    </block>
  </view>
  <view class='no_data' wx:else>暂无待审核图书</view>
</view>

